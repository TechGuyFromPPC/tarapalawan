// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // We can leave experimental empty now that your root directory is fixed!
  experimental: {},
};

export default nextConfig;